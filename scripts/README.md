# scripts/

This folder contains utility and development scripts for the project.

- **mock-frontend.ts**: Runs a minimal mock frontend server (using Hono) for local development. It provides a `/verify-email` page to simulate the email verification flow, allowing you to test backend email verification links without a real frontend.
